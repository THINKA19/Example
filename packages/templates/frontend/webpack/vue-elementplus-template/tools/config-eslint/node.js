import { createConfig } from "./factory.js";

// The only variant currently exercised by real projects (example-app, example-lib).
export default createConfig({
  // No Node-specific overrides needed yet.
});
