# pnpm 场景下,允许幽灵依赖能被 node_modules 顶层解析到(按需开启)
shamefully-hoist=true

# 强制校验 package.json 里的 engines 字段,版本不匹配直接报错
engine-strict=true

# 公司内部私有源(如无可删除或替换为公司地址)
# registry=https://registry.your-company.com/
